#include<stdio.h>
void main()
{
int a[10],i,j,opt,t;
int ele=sizeof(a)/sizeof(a[0]);
printf(" Enter the array element: ");
for(i=0;i<ele;i++)
scanf("%d",&a[i]);

printf(" ARRAY : ");
for(i=0;i<ele;i++)
printf("%d ",a[i]);
printf("\n");

printf(" Enter the Opt for sorting array\n 1-for ascending order.\n 2-for decending order.\n");
scanf("%d", &opt);

for(i=0;i<ele-1;i++)
switch(opt)
     {
      case 1: if(

       }


}
